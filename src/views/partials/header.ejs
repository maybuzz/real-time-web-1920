<header class="header">
  <a class="btn btn--back" href="/">get-a-room</a>
  <a class="header-img">
    <% if (recent.items) { %>
      <img class="" src="<%= recent.items[0].track.album.images[0].url %>" alt="">
    <% } else { %>
      <img class="result-img" src="/img/placeholder.png" alt="placeholder">
    <% } %>
  </a>
</header>
