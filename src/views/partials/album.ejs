<section class="album">
  <div class="album-cover">
    <img class="album-cover-img" src="<%= album.images[0].url %>" alt="">
  </div>
  <div class="album-tracks">
    <h3><%= album.name %></h3>
    <ol class="album-tracks-list">
      <% if(tracks.length !== 0) { %>
        <% tracks.forEach((track) => { %>
          <li class="album-tracks-item">
            <a href="/room/:id/<%=track.uri%>"><%=track.name %></a>
          </li>
        <% }) %>
      <% } %>
    </ol>
  </div>
  <div class="album-controls">
    <!-- <div class="album-controls-play"> -->
      <a id="play" class="album-controls-play controls-btn" href="/room/:id">
        play
      </a>
    <!-- </div> -->
  </div>
</section>
